let fibonacci = fn(x) {
    if x < 2 {
        return x
    } else {
        return fibonacci(x - 1) + fibonacci(x - 2)
    }
};
let start = time()
fibonacci(36);
print("cost: " + (time() - start) + " ms")